// 2D semi dynamic array (non jagged) - where 
// number of rows are fixed and 
// number of columns for each row are dynamic
// Another option: Same thing could be done
// more elegantly with a vector inside a vector

#include <iostream>
using namespace std;

int main()
{
	//create a 2d square array
	int count = 10;
	int *a[count];
		cin >> count;
	

	return 0;
}